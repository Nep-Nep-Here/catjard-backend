package com.catjard.solicitudes.model;

// Tipo de cambio (PDF): estandar (repetitivo), normal (nueva funcionalidad), emergencia (correccion critica).
// Define el prefijo del codigo: CHE (estandar), CHN (normal), CHM (emergencia).
public enum TipoCambio {
    estandar, normal, emergencia
}
