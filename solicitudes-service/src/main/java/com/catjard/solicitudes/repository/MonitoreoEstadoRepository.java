package com.catjard.solicitudes.repository;

import com.catjard.solicitudes.model.MonitoreoEstado;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MonitoreoEstadoRepository extends JpaRepository<MonitoreoEstado, Long> {
}
